import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#fa8993',
      main: '#862951',
      dark: '#aa525f',
    },
    background: {
      default: '#FFFFFF',
    },
  },
});

export default theme;
